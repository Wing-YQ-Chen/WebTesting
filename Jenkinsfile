pipeline {
	agent any
    stages {

		stage('Build') {
			steps {
				bat """
				echo "Build"
                """

            }
        }


		stage('Run Testing') {
			steps {
				bat """
				echo "Run Testing"
				"""
			}
		}
    }
}